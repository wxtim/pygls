{
  "name": "cylcls",
  "description": "Simple json extension example",
  "author": "Open Law Library",
  "repository": "https://github.com/openlawlibrary/pygls",
  "license": "Apache-2.0",
  "version": "0.11.3",
  "publisher": "openlawlibrary",
  "engines": {
    "vscode": "^1.62.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onLanguage:cylc"
  ],
  "contributes": {
    "commands": [
      {
        "command": "registerCompletions",
        "title": "Register completions"
      },
      {
        "command": "unregisterCompletions",
        "title": "Unregister completions"
      }
    ],
    "configuration": {
      "type": "object",
      "title": "Cylc Server Configuration",
      "properties": {
        "cylcServer.exampleConfiguration": {
          "scope": "resource",
          "type": "string",
          "default": "Cylc Language Server."
        }
      }
    }
  },
  "main": "./client/out/extension",
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -b",
    "watch": "tsc -b -w"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "@types/vscode": "^1.62.0",
    "@typescript-eslint/eslint-plugin": "^5.3.0",
    "@typescript-eslint/parser": "^5.3.0",
    "eslint": "^8.2.0",
    "pyright": "^1.1.292",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "vscode-languageclient": "^7.0.0"
  }
}
